projectRoot="/opt/checkouts/esb_voddai"

build {
    build_args=""
}

jira {
    update_jira="false"
}

build_package {
    artifacts {
        name {
            tar="esb_voddai.tar"
        }
        location {
            tar="$projectRoot/build/"
        }
    }
}
