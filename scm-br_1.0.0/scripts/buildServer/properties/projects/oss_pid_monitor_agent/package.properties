projectRoot="/opt/checkouts/oss_pid_monitor_agent"
commonTargetRoot="target"

artifacts {
    name {
        war="oss_pidma.war"
        config="*.*"
        deploy="deploy.txt"
    }
    location {
        war="$projectRoot/$commonTargetRoot/"
        config="$projectRoot/config"
        deploy="$projectRoot"
    }
}
