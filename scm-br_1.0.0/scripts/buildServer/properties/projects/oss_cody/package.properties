projectRoot="/opt/checkouts/oss_cody"
commonTargetRoot="target"

artifacts {
    name {
        war="oss_cody.war"
        config="*.*"
        deploy="deploy.txt"
    }
    location {
        war="$projectRoot/$commonTargetRoot/"
        config="$projectRoot/config"
        deploy="$projectRoot"
    }
}
